# Cron entries owned by the coolify service. Assembled into /etc/cron.d/iac
# on the ops LXC by tools/lib/assemble-crons (called by apply.sh phase 8).
#
# (Currently empty — Coolify API token rotation is now operator-driven via
# `services/coolify/rotate-token.sh`, since it edits iac/secrets.sops.yaml
# and the change must be committed + pushed by a human, not a cron.)
[]
